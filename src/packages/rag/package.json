{
  "name": "@rag/core",
  "version": "1.0.0",
  "description": "RAG functionality using Mastra",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "lint": "eslint src --ext .ts",
    "type-check": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "mastra": "^0.10.13",
    "drizzle-orm": "^0.44.3"
  },
  "devDependencies": {
    "@types/node": "^24.0.15",
    "typescript": "^5.8.3",
    "drizzle-kit": "^0.31.4",
    "vitest": "^3.2.4",
    "@vitest/ui": "^3.2.4",
    "dotenv": "^16.4.5"
  }
}